'use strict';

/**
 * Your helper module.
 * 
 * Example usage (in controller):
 * mod.helpers.example(myArguments)
 * 
 * @param {*} myArguments - Your arguments | You can make more than one argument
 */

module.exports = function (myArguments) {
  return true;
};

/**
 * Another example, you can use module method.
 * 
 * Example usage (in controller):
 * mod.helpers.example.myMethod(myArguments)
 */

// exports.myMethod = function (myArguments) {
//   return true;
// };